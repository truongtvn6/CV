This is LaTeX template for a CV(Curriculum Vitae), Résumé or Cover Letter 

#### Requirements
1. You need to install docker and run it
2. Run this cmd: 
- For MAc: docker run -u $UID:$GID --rm -v $PWD:/work csmith/\awesome-cv-builder
- For Windows:  docker run --rm -v ${PWD}:/work csmith/awesome-cv-builder
3. Then you can see the output: cv.pdf
